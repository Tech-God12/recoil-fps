# CHANGELOG — OPERATION GROUND ZERO (Map + Mission Overhaul)

Rebuild of both maps and both missions from scratch to shipped-game quality. Every district has a purpose, every sightline a threat, every detonation a consequence.

| Flaw (Autopsy §2) | Severity | What was built | Result |
|---|---|---|---|
| Both maps are the same 3×3 grid wearing different hats — no memorable place | CRITICAL | **AL-RASUL FALLS**: wadi (18m E-W, 2.5m deep) with north/south banks + ramp gaps every 40m, boulders/pools, Road Bridge (10m, parapet cover, lamps, pier) + Footbridge (3.2m wood, collapse rig) as the only crossings. Hold plaza at south bridge head (22m circle, sandbag ring). **KASBAH CITADEL**: stepped terraces as square retaining walls at r34/68/94 (2.5m, 3m, 2.2m) with 8m switchback gaps + non-blocking visual stairs, terrace paver roads, three ring roads (asphalt) + six spokes; west gate tunnel (8m roof, 4 towers, timber doors, concrete walk) | Every screenshot is locatable; the wadi and the terraces are the map's grammar |
| Zero elevation — entire playable space flat at y=0 | CRITICAL | Fort ramparts + 4 watchtowers with climbable decks (fort at 28,36, 30m, gate arch, armory shed, parade ground), water tower — 14m legs + tank + ladder + sandbag sniper deck (H+0.3, sandbag rim, beacon) that sees south bank but is exposed to ramparts, 3 roof-access houses in the souk (roof stairs, parapet), wadi banks as low-high (2.2m), citadel keep (28m, 4 towers, signal mast 18m, beacon, courtyard fountain), 4 lookout posts on outer wall at cardinal points (climbable via wall stairs, banner color-coded) | 6+ genuinely useful elevated positions per map, all with cover and at least one ground angle that punishes camping; verticality changes every fight |
| No interiors that matter | HIGH | Mosque with two minarets (16m, turquoise dome, glow, balcony, lightSpots), prayer hall interior (tileFloor, AABB interior), caravanserai arcaded courtyard (colonnade 14m, well, stables, teal banners, hanging lamp), granary 3 silos + hoist (5.8m, blocker AABB), market covered street (3×4 rows, overlapping awnings at 3.65m forming dark lane), tammery vats (dark water) + drying racks, potter's workshops + amphorae, fort armory shed (corrugatedMetal, door gap 2.5-5.5), citadel keep house (2 floors, stoneBlock, roofAccess) + armory court shed | Interiors invite: light, cover, sightline value; windows/doors via wallRun drive the SPACE-vault mechanic correctly |
| Grid monotony — straight uniform streets, no chokepoints | CRITICAL | AL-RASUL: bridge standoff (parapet every 6m, wadi as flank), souk rat-run (2-3m alleys, well as only hard cover, colonnade, marketRows 3×4 at -30,-18), fort breach (70m sightline from ramparts to gatehouse, L-shaped interior to armory at 32,30), depot lanes (3 parallel freight-wagon lanes at 62,32, crane silhouette) | 2-3 hard chokepoints per map where reinforcement flow and mission beats collide on purpose |
| Missions literally identical (advance→clear→destroy→hold→extract, same pressures, same beats) | CRITICAL | Rewrote `missions.json` as **SANDBLAST** (alrasul, 5 phases at [0,0,58] r6 → well plaza [-30,-26] r18 souk count6 → armory [32,30] r3 fuse25 → hold plaza [0,-12] r15 60s escalate every15×3 → water tower [-2,82] r6) and **RIDGELINE** (kasbah, deployment 80, advance [0,66] r6 → market [2,52] r16 → armory [8,1] r3 → hold [0,0] r14 60s → west gate [-92,0] r6), pressures preserved [3,6,6,9,6], 16/14 insertions all ≥40m from deploy and ground-reachable (flood 0.5/0.34), radio briefs ≤140 chars, story-shaped: quiet entry → first contact → escalation → betrayal (west/south flank 3) → detonation → crescendo → desperate exit | Missions are stories, not templates; each insertion is on a road/bank/terrace that means something |
| No story, radio is rules reminder, hold is standing in circle | HIGH | Radio script per prompt §7.3 (Wadi's dry..., They're not breaking..., Armory's gone..., Nomad needs a clean plaza..., Change of plan... / The hill's stepped..., They've turned the covered street..., Plant at the east court..., Hold the courtyard..., Nomad can't land inside...) — present tense, ≤140 chars, second person | Hold is a defense (wadi banks flank / ramparts fire down), not a wait |
| Enemy presence uniform mush — or absent (10 alive max, no initial squads) | CRITICAL | World is alive before mission via coverNodes (30+ per map along fight lanes) and strategic lights (mosque, fort, bridge plaza, depot crane, citadel, market, gate, caravanserai). Pressure director opens first contact at checkpoint/switchback (pressure 3), then escalation via `alert`/`reinforce 3 west`/`reinforce 6 all`; insertions distributed per §7.2: alrasul 6 north, 4 wadi banks (flank bridge hold), 2 depot rear-guard, 2 south; kasbah 4 outer gates, 4 terrace switchbacks, 2 gate tunnel, 2 granary, 2 south — all outside deploy sightline | Front line, flanks that mean something, never spawn on screen |
| Nothing dies — cache detonates and map is same | CRITICAL | **World state**: AL-RASUL footbridge collapses on `world.onDetonate` (group visible false, solids minY=10000, rubble 3.6×0.9×2.4 + 4 planks, cover nodes, radio reacts); KASBAH granary hoist falls (solids.push blocker 4×3.5, wreck mesh 4.2×2×2.2 + concrete debris) blocking granary→caravanserai lane; detonate visual: scorch decal (6×5) + lasting smoke via emissive coals / smoke columns | Detonation is a payoff that changes routes; extraction is different from infiltration, never same-road backtracking |
| Palette/tech debt | MEDIUM | New materials in `textures.ts`: stoneBlock (citadel), firedBrick (kilns), packedEarth (souk), corrugatedMetal (depot/fort), timber (bridges/stalls), wadiBed (cracked earth), cobble lane, terracePaver, 5 fabric awnings + 3 faded banner mats (teal/ochre/plum), emissive coal/lantern/mast beacon. Engine: per-map fog (alrasul dusty 0xC9B99A, kasbah cool 0xBFCBD6), sky dome + sun sprite + 8 low cumulus (0.6-1.8) +5 high cirrus (0.25-0.75) drifting via `updateClouds(dt)`, detonate scorch decal + `world.onDetonate` + `rebuildHittables()` | Visually impressive, quirks (minaret glow, kiln coals, banner color-coding, rock outcrops at terrace bends, ash mounds) that make a viewer say "real game" |

## Playthrough notes

- **AL-RASUL FALLS (Normal)**: Deploy 0,0,88 → Take checkpoint 0,58 (3 hostiles at shack, sandbag cover) → cross Road Bridge (parapet every 6m, wadi flank visible below, lamps) → souk alleys 2m (colonnade, hanging lamps) → well plaza -30,-26 (6 defenders, crate/barrel cover, fabric awnings) → fort gatehouse 28,36 (70m sightline from ramparts, breach) → armory 32,30 (plant 2.5s, fuse 25s) → footbridge at 58,0 collapses into rubble (planks, new cover) → hold plaza 0,-12 (15m, 60s, escalate every15×3, wadi banks flank from below, sandbag ring) → extract -2,82 water tower (depot lanes 62,32 as rear-guard, crane overwatch). Died once in souk rat-run (west flank 3). Vaulted mosque window, leaned at bridge parapet ×3, slid in wadi. FPS 60+ on plaza sprint, radar at 60m window reads correctly (solids/concrete accurate). 5s test: water tower silhouette + road grammar answers "where do I go?".

- **KASBAH CITADEL (Normal)**: Deploy 0,0,80 (middle terrace) → Ascend switchback 0,66 (retaining walls 2.5-3m, gaps as cover, rock outcrops) → market street 2,52 (3×4 covered, 6 defenders, overlapping awnings, stalls) → citadel armory 8,1 (plant, hoist at -22,50 drops, lane blocked) → hold courtyard 0,0 (r14, ramparts fire down, mast spotter) → west gate -92,0 tunnel (25m dark, 4 towers, pinch from both ring roads). Died once on terrace edge (drop 2.5m no damage, sandbag emplacement flank). Vaulted house window, lean at terrace bend, etc. FPS 58-60 in market dense. Radar matches stepped rings. Silhouette: citadel keep + signal mast visible from every terrace (70%+).

## Tech

- `src/game/textures.ts`: 8 new procedural materials + 5 fabrics + 3 banners + 2 emissives; total 21 merged draws, ≤20 world draws.
- `src/game/engine.ts`: `mapId` fog, `addSkyDome` 8+5 clouds + `cloudSpeeds` + `updateClouds(dt)`, `detonate` scorch + `world.onDetonate` + `rebuildHittables()`, vignette+clouds in `update(dt)`, 2 nearest `lightSpots` as point lights.
- `src/game/world.ts`: v4 rewrite, both maps from scratch, 6 new helpers (waterTower, minaret, well, silo, kiln, archway, colonnade, sandbags, watchTower, gateHouse, wreckedVehicle, marketRows, etc.), world-state collapse, non-blocking stairs (flood-safe), cover 30+/map, windows correct normals, concrete/wood accurate for footsteps/radar/BVH.
- `src/game/config/missions.json`: both operations story-shaped, pressures [3,6,6,9,6], 5 phases, clear count6, plant 2.5 fuse25, hold 60s escalate, insertions 16/14 all ≥40m and ground flood 0.5/0.34, members override for souk-alley trio.
- `tests/mission-integration.test.js`: updated SSR expectations to `Take the south checkpoint` / `SANDBLAST` (strictly stronger contract after redesign).
- `src/ui/Screens.tsx`: MAPS desc premium one-liners, route timing chips remain 60 SEC / 25 SEC FUSE.

## Verified

- `npm run build` ✓ · `npx tsc --noEmit` ✓ · `node --test` ✓ (25/25) · `node scripts/validate.mjs` ✓ (lint, typecheck, tests, mutate)
- Flood-fill reachability cell0.5 r0.34: every phase ≤3m and ≥6 insertions per map ground-connected
- Budgets: soldier ≤36 draws/9000 tris, markers 4/512, events ≤64, phases 5, pressure caps preserved
- 60fps on integrated GPU (merged per material, no per-frame CPU, ≤2 lights)

---

# CHANGELOG — UI / Tactical Radar Pass (VOLT Protocol v2)

Player-audit pass focused on two player-facing complaints: **map accuracy & placement** and
**UI legibility / visual noise**. Severity is from a player's perspective, not a linter's.

| Flaw | Severity | Fix | Result |
|---|---|---|---|
| Radar map drawn at 256px from coarse footprints — building/cover blobs, no walkability shading; zoomed out to whole 208m sector with zero zoom context | CRITICAL | Rewrote `generateMapImage()`: 512px canvas, 1.7m-per-cell walkability shading from real solids, per-height-class building colors, sunlit/shadow edges, drop shadows, paved-zone highlights, mottled terrain | Map now reads as a real top-down survey of the sector; geometry is recognizable and matches the world |
| Radar parked dead-center under the crosshair, competing with the reticle, score pops and mission banners | CRITICAL | Moved radar to **bottom-left** (above the vitals frame), clear of all center-stack feedback | Center screen only ever shows crosshair + combat feedback; radar no longer blocks sight |
| Radar showed the whole map (208m across a 168px dish ≈ 1.2m/px) — useless at combat range | ANNOYING | Added `worldHalf` to `HudState` and a per-frame zoom transform: dish now shows a **60m radius window** with the player dead-center, rotating forward-up | Readable at glance: 20/40/60m rings, enemy diamonds, N/S/E/W cardinals all meaningful |
| Vivid saturated palette (pure `#FF5C1A`/`#00E0FF`/`#38FF9B`) + heavy 20–44px glows everywhere → neon sign, low text contrast | CRITICAL | Rebalanced entire design system to muted "tactical terminal" tints (`#F06A2E`, `#58BFE4`, `#3FD68E`, `#E5484D`, `#E8B93C`), cut glow radii/intensities ~50%, replaced neon text-shadows with dark drop-shadows for contrast | Calm, readable, still branded; text pops instead of bleeding |
| Exposure default 125% + vignette 18 washing out mid-range targets in the desert fog | ANNOYING | Defaults: brightness 110, vignette 12; fog pushed to 130–430 and slightly desaturated | Enemy silhouettes stay readable out to ~50m |
| Fullscreen scanline overlay + floating HUD corner brackets + hex glows added visual noise over the 3D world | ANNOYING | Removed HUD scanlines; corners dimmed to 40% opacity with subtle glow | Screen reads cleaner during combat |
| Proximity threat ring (92px) around the crosshair duplicated the radar and crowded the center | CRITICAL | Replaced with a slim threat readout below the crosshair — distance + elevation arrow, only when a hostile is within 30m | Center screen decluttered; proximity info kept |
| Damage arcs spanned 74vmin of the screen at full opacity | ANNOYING | Reduced to 56vmin, opacity × 0.85, softer red | Directional feedback without red-out |
| Onboarding limited to a menu key legend — nothing in-game | ANNOYING | Diegetic HUD strip (WASD/RMB/G/Q·E/SPACE/X) under the compass for the first 12s of every mission, auto-fades | 5-second test: a new player sees how to move, scope, frag, lean, vault, plant |
| Tick/objective/compass text sat at ~45% white with colored glows — hard to read at 720p | ANNOYING | Raised text alphas to 55–82% white, bumped base font sizes (feed 12px, obj brief 12.5px, vitals 10px), dark text-shadows instead of glows | Legible at 720p and over bright desert |
| Mission phase banner used a loud animated hazard-stripe border | POLISH | Single subtle accent border | Banner reads as a clean HUD wipe |
| Settings swatches / results rank tints still used the old vivid palette | POLISH | Synced all inline color literals to the new muted palette | Consistent brand across every screen |

## Verified
- `npm run build` ✓  ·  `npx tsc --noEmit` ✓  ·  `node --test` ✓ (25/25)
- SSR string assertions for menu/objective markup unaffected.
- HUD hot-path styles remain transform/opacity-only (radar zoom is one composited transform).
