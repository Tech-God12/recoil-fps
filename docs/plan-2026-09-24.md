# Plan — 2026-09-24: Anti-Lag Headline + Polish Pass

**Session branch:** `arena/01a0d5cc-recoil-fps`  
**Headline (S1, system):** **ENGINE ANTI-LAG PASS** — make the player's "super laggy" report impossible on mid hardware. Target: **stable 60 FPS median**, **~40% lower frame time**, **draws -45%**, **zero per-frame allocation in hot paths**, **GC stalls eliminated**.

**Why this headline:** every other polish is invisible if the game stutters. Audit R6 is the single easiest-to-prove "before/after" (FPS median, draw count, allocations) and the reporter's P0.

---

## Headline scope — Anti-Lag Pass (ship as one system)

| Area | Before | After | How verified |
|------|--------|-------|--------------|
| Renderer AA | `antialias:true` + composer (waste) | `antialias:false` (composer handles AA) | code + visual |
| Shadow init | 2048 always, bias fixed | init 1024, bias scaled by mapSize, caster whitelist, needsUpdate only if moved >0.8 m | code + headless test |
| Post chain | 4 passes always allocated | conditional: bloom pass only if `s.bloom`, vignette only if `vignette\|grain>0`, `postFxOn` gates composer vs direct render | code |
| DPR | cap 1.25 | cap 1.1, adaptive still works | code |
| Anisotropy | 8× always | 4× baseline, 8× only on High | code |
| Dust | 320 points every frame | 180 baseline, 120 potato, throttled every 2nd frame | headless count + visual |
| Decals | 260 individual meshes (260 matrix updates) | 2 InstancedMesh (holes + blood) = 2 draws | headless test draw count |
| Weapon draws | 30–44 draws / 13k–47k tris | merge per material → ≤18 draws, segment cut 24→16 non-hero → <22k avg | `weapon-assembly.test.js` budget |
| HUD poll | 50 ms all (20 Hz) with 73 compass nodes + allocations | split: 20 Hz vitals, 5 Hz radar, memo compass, reuse buffers | headless poll test |
| AI GC | allocates per tick | scratch vectors `tmpV/tmpV2` reused | headless alloc counting |
| Audio panners | leaks if quiet | prune in `updateListener`, limit 16 active | unit test |
| Shake | RNG per frame jitter | trauma smooth decay (seeded) | unit test |

**Ref:** Audit R6, R7, R9, R10, F6, F7, C etc.

### Files touched (headline)
- `src/game/engine.ts` — renderer, shadows, composer, DPR, anisotropy, shake
- `src/game/effects.ts` — instanced decals, dust count/throttle, brass already good
- `src/game/world.ts` — caster whitelist, perimeter prop shadows flag
- `src/game/ai.ts` — scratch vectors
- `src/game/audio.ts` — panner prune
- `src/ui/Hud.tsx` + `src/App.tsx` — memo compass, split poll
- `src/game/weapons/*.ts` + `src/game/models.ts` — batch/LOD (draw merge)
- `tests/*` — new `performance-opt.test.js`, updated budgets

### Success criteria
- `npm run build` single-file still ≤+2% gzip vs gunfeel (5,258 kB base) — we add code but remove draws, not assets.
- Headless: world tri budget unchanged, weapon draw budget pinned lower, effects draw 0→0→2 (instanced) with no leak, shadow refresh gated, no per-frame allocation in `checkLOS`/`fireShot`.
- In-browser (when available): FPS median >55 on integrated GPU, frame time p95 <22 ms.

---

## Polish bundle — 12 items (individually small, collectively "finished")

| # | Audit | Item | Effort | Evidence |
|---|-------|------|--------|----------|
| P1 | W6 | Viewmodel batch + LOD (cuts draws 30→≤18, tris -35%) | M | `weapon-assembly.test.js` before/after table |
| P2 | W7 | Material split metal/polymer/wood metalness | S | code + `weapon-finishes.test.js` |
| P3 | R8 | Per-map exposure + bloom retune (desert 1.0, Sirocco 1.12, thresh 0.96) | S | screenshot pair / code |
| P4 | R10 | Aniso 8→4 baseline | S | code, headless maxAniso test |
| P5 | F6 | HUD memo compass + split poll rates | S | headless poll count |
| P6 | G13 | Trauma shake (smooth) | S | unit test seeded |
| P7 | G10 | Per-weapon recoil patterns (data only, no TTK drift) | M | `ttk-bands.test.js` still passes |
| P8 | U9 | HUD vitals grouping + contrast plate | S | screenshot pair |
| P9 | D6 | Spatial panner leak fix + limit 16 | S | `audio-mix.test.js` |
| P10 | A11 | AI scratch vectors zero-alloc | S | `ai-alloc.test` |
| P11 | R7 | Shadow bias scale + caster whitelist | S | code |
| P12 | F7 | Instanced decals (paired with R9) | M | draw count test |

**All polish have before/after:** screenshot pair if browser else measured number/test.

---

## What we are NOT doing (and why)

- **Kits / abilities** — another session is building kits in parallel; would collide (per user request).
- **New map Ridgeline + lane retune** — needs art/iteration, too risky with lag as P0.
- **True ragdoll, full PBR hands, killcam, full stats persistence, radio-delay AI** — all L/M and need tuning/art; doing stubs (glove boxes, death card follow-up, GC only) instead.
- **Backend MP / dailies / economy rebalance** — out of scope for browser-only FPS.

---

## Verification checklist (definition of done)

- [ ] `docs/audit-2026-09-24-lag.md` present, 30–60 findings, all 12 categories.
- [ ] This plan file present before production code.
- [ ] No TODOs/stubs; every number commented with reasoning.
- [ ] Before/after screenshot pair or measured number/test per polish.
- [ ] Headless tests new + existing: lint, typecheck, 260→~280 tests, mutations, build pass. Pre-existing failures listed.
- [ ] `CHANGELOG.md` updated in repo style, `docs/performance-overhaul.md` written.
- [ ] Self-review: re-read diff as harsh critic, list 5 still-broken, fix ≥3, re-run.

## Report (end of session)

Will end with: audit summary counts, what shipped (headline + polish), before/after evidence table, test results, honest "still broken" list. User gets concise simple summary plus optimization proof.
