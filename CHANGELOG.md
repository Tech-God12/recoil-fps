## 2026-09-25 — UI declutter: fewer chrome, no shouting

Cut the decorative layer out of every screen. The 01/02/03 index badges are gone from the home nav, theater cards, arena cards, mission list, weapon rack, timeline and pause actions; mission/round/phase readouts now say "Objective 3 of 6" instead of `03 / 06`. Arena Mode no longer asks for anything: the side is a coin flip at spawn and every match is first to 7, so the side/length segmented control, the five-item BUY / PLANT / DEFUSE / SURVIVE / COMMAND rules strip and the version-stamped footer are all deleted — the screen is now two cards and one Play button. The Warehouse loadout lost its armor picker (everyone spawns in light plating via `TDM_PLAYER_ARMOR`), the Bravo roster preview, the loadout-rules box and the whole bottom bar; Play moved into the header and the grid went three columns to two. The Armory's three-step "field manual" tutorial is gone, along with `seenArmoryTutorial` in the saved profile. Every fully capitalised label across the menus, HUD, buy menu, scoreboards, banners, radio lines and ranked screens is now sentence case (69 `text-transform: uppercase` declarations removed), with genuine acronyms — NATO, ACP, ADR, MVP, HS, UAV, C4 — left alone; the player's roster name is `You` rather than `YOU`, so the boards no longer read "You (YOU)". 76 dead CSS rules for the removed elements were dropped with them.

Verification: `node scripts/validate.mjs` steps — eslint clean, `tsc --noEmit` clean, 281/281 Node tests, 44/44 mutations killed; `npm run build` — single-file HTML 5,255.65 kB / 3,098.63 kB gzip (down from 5,276.09 kB). Screens were verified by SSR-rendering the real components (`MainMenu`, `Armory`, `TdmSetup`, `RankedSetup`, `DefusalHudLayer`, `BuyMenu`, `PauseMenu`, `BootScreen`) and diffing their visible text. No browser was available in the sandbox, so grid balance, letter-spacing and hover states need human eyes; `tests/defusal-ui.test.js` now pins that the arena screen no longer emits `df-options`, `df-rules`, `map2-num` or "first to 13".

## 2026-09-24 — Headlines: Frame Budget (performance) + impact & death reactions

Frame Budget: decorative point lights are now virtual sources served by a fixed two-light pool (`light-budget.ts`), so shader light count no longer grows with map dressing (Warehouse 8 → 4 per pixel). The vignette moved to a CSS overlay, so default settings skip the whole post chain and render straight to the multisampled canvas (the world is antialiased again). The sun shadow box follows the player, texel-snapped at ±50 m (`shadow-fit.ts`) — sharper and cheaper; terrain is receive-only. Bot rifles swap to a one-draw LOD past 14 m, invisible hit proxies no longer draw, and the AI LOS/patrol hot paths stop allocating. Settings names the biggest FPS levers, and the FPS chip shows the adaptive render scale. Impact reactions: one shared `reactions.ts` gives every soldier a zone-aware directional flinch and five multi-joint deaths (crumple, blown back, pitched forward, spun, kneel, blast) chosen from hit zone, shot direction and cause; falls are wall-aware (turn or slump instead of clipping), the rifle drops and clatters, and a spatial thud lands. Hit direction is fed from bullets, frags, streaks and C4. Audit: `docs/audit-2026-09-24-b.md`; design notes `docs/frame-budget.md`, `docs/impact-reactions.md`.

Verification: `node scripts/validate.mjs` — lint, typecheck clean, 281/281 Node tests, 44/44 mutations killed; `npm run build` 5,276.09 kB / 3,103.57 kB gzip; new `tests/frame-budget.test.js` (10) and `tests/impact-reactions.test.js` (11). `scripts/perf-probe.ts` (real Engine on stub WebGL, 600 frames/map): Warehouse draw calls 203 → 165 median, triangles 143k → 105k; Sirocco 139k → 103k tris; Sandblast 267k → 231k. `scripts/reactions-engine-sim.ts` kills bots on two maps with real weapons and checks variants, sounds, settle and respawn reset. `defusal-engine-smoke.ts` now passes `mode: 'defusal'` explicitly — it previously relied on the random side roll landing the way it wanted. No browser was available: GPU timings, light cross-fades and death feel need human eyes.

## 2026-09-24 — Headline: gunfeel (TTK rebalance, viewmodel, shot effects, mix, ranked reachability)

AWM one-taps chest (78→160 dmg: 1 shot in missions, bolt-actions exempt from the 74-dmg TDM cap so the arena is 1 tap vs armor-0 / 2 taps vs armor-1/2); SCAR dethroned to AK parity (52→48, 3-shot body, keeps 1-tap head + 45 m × 0.9 identity); masterkey fixed to its 8×12 spec. Viewmodel rescaled (1.95→1.35) with a cool fill light, tamed muzzle flash, and ADS geometry proven headless for all 10 guns. Every shot now puffs pooled muzzle smoke and ejects a bouncing brass casing with a delayed tink; kills land with 45 ms hitstop plus a crosshair pulse; the master bus runs through a glue compressor. Streaks re-costed to 300/600/850/1200/2000 (3/6/9/12/20 kills) with streak keys disabled in ranked. HUD ammo states are relative (a full AWM no longer blinks RELOAD), the bind reference is complete, and menu keyboard nav reaches all five entries. OPERATION BLACKOUT is reachable end-to-end (menu → setup → launch → debrief with cash ledger → re-queue), pause/results restart preserves mode (mission/TDM/ranked/defusal), the threat readout no longer sees through walls, shadows refresh every 10th frame, and headshot feed/stats take an explicit flag instead of guessing by damage.

Verification: `node scripts/validate.mjs` — lint, typecheck clean, 260/260 Node tests (32 new), 44/44 mutations killed; `npm run build` — single-file HTML 5,258.93 kB / 3,098.52 kB gzip (+0.08% vs main `faa5214`). Rebased onto the defusal update: the ranked imports, debrief outcome, comp `hud()` guard, and 4-mode AI-roster pin had already landed upstream and were kept, not re-applied. No browser was available: viewmodel pose, smoke/brass density, hitstop feel, and compressor settings are geometrically/unit proven but need human eyes; see `docs/headline-gunfeel.md` for the blind-spot list.

## 2026-09-24 — Honest wallet, validated settings, and truthful match results

Removed the automatic $9,999,999 wallet top-up from normal startup; saved cash and ownership are left untouched. The explicit `#cash=` test grant is now capped and development-only. Persisted settings and live UI patches pass through one sanitizer that clamps numeric ranges, validates difficulty/map/shadow choices, checks crosshair colors and drops unknown values; the resolution-scale control now agrees with its supported 50–100% range. The deploy screen no longer invents percentage progress or completed loading milestones: it uses an accessible indeterminate bar and rotating, actionable gameplay tips. TDM accounting is now separate from story mission counts; match outcomes are explicit, and a tie receives a $250 draw stipend, a draw-specific debrief and a Rematch action rather than a grade-D defeat presentation. Story mission payout/count behavior and TDM victory payout remain covered.

Hostile-playtester audit: malformed numeric settings and forged enum/color values clamp or fall back; legacy high balances are preserved unless the player explicitly confirms a current-cash-only reset (ownership and lifetime earnings remain). A tied match was exercised through engine event emission, wallet settlement and rendered debrief, including the non-loss draw path and unchanged story-mission count. Reduced-motion mode keeps a visible static loading cue. No human playtest was performed.

Verification: `node scripts/validate.mjs` — lint, typecheck, 132/132 Node tests, 34/34 mutations killed; `npm run build` — single-file HTML 4,566.63 kB / 2,697.22 kB gzip. Browser and hardware checks were not run.

## 2026-09-24 — Economy rebalance, weapon fit fixes, validation restored

Reconciled the stale preflight contracts against the shipped 10-gun / 59-part catalog without reducing current content: each attachment now has a pinned draw budget, the 119 actually supported weapon/slot combinations are audited, and rail/barrel/SPAS checks only exercise compatible parts. Compact rail lasers now seat against their mounting shoes instead of floating. Rebalanced listed weapon and attachment prices by half (catalog $126,700 → $63,350), matching the tested $3,350 competent run to roughly 19 runs for a full unlock; existing ownership and saved balances remain unchanged. Results now itemize the TDM SHUT DOWN bounty. Mutation probes target unique current code paths and run the owning test file, preserving all 34 behavioral kills while avoiding unrelated suites. Updated only assertions proven stale (catalog count, map list, capitalization, names, supported slots and fit IDs) and replaced them with exact current-state checks.

Verification: `node scripts/validate.mjs` — lint, typecheck, 121/121 Node tests, 34/34 mutations killed; `npm run build` — single-file HTML 4,562.03 kB / 2,695.82 kB gzip.

## 2026-09-23 — BOMB DEFUSAL: CS2-style competitive mode on the new Sirocco map

New flagship mode under Arena Mode: a round-based 5v5 **Bomb Defusal** match on **Sirocco**, a purpose-built 88 m desert town (A long / mid with short and the B-window connector / roofed B tunnels, two painted bomb sites, screen walls that kill the spawn-to-spawn sightline, golden-hour grade, minaret landmark, new key art). Rounds follow CS2: 10 s freeze + buy, 1:45 clock, 3.2 s plant, 40 s fuse with accelerating beeps, 10 s defuse (5 s with a kit), no respawns, halftime side swap, first to 7 (short) or 13 (full), draws at 6-6 / 12-12. The economy is CS2's to the dollar: $800 start, $16k cap, $3,250/$3,500 wins, $1,400→$3,400 loss ladder with the pistol-round loser on $1,900 and wins stepping the ladder down one tier, +$800 team plant bonus, +$300 planter/defuser, $0 for attackers who hide out the clock, kill rewards by class ($600 SMG, $900 shotgun, $100 AWM). New **buy menu** (B) with real 3D gun thumbnails, CS number-key flow, auto-buy, side-locked AK-47 / M416 and kits — and every gun you own in the Armory arrives with **your build**. Survivors keep their kit; the dead drop their best gun and anyone can pick it up (X). New utility: **smoke grenades** (Z) that genuinely block bot sight lines, plus drop-bomb (5) and radio orders (6/7 call A/B, 8 follow me). CS damage model (head ×4, legs ×0.75, per-weapon armor penetration: AK and Deagle one-tap helmets, M416 doesn't, AWM kills to the body), no regen. Bots are a real team: pistol/eco/force/full buy calls, role-based shopping, attack plans (execute, split, rush, default, fake) walked along authored lanes with staging, execute smokes and flashes, bomb fetches, post-plant holds; defenders set up 2-1-2 or stacks, hold angles with a pre-aim bonus, turn to nearby gunfire, step out of smokes, smoke chokes, rotate on reads, retake together, defuse — or save when the fuse makes a defuse impossible; bots visibly carry what they bought and wear side colours. HUD: CS2 top bar (scores, alive pips, clock that becomes a pulsing C4), round banners with MVP, money and kit, plant/defuse bars, spectator chase-cam with death cam, smoke white-out, radar that shows only spotted enemies plus sites/allies/bomb, Tab scoreboard with money/K/A/D/ADR/HS/MVP and a round-history strip; new pause card and a debrief with round history and standings. The shared TDM bot brain gained director hooks, weapon profiles, a sight cone and smoke-aware LOS without changing TDM behaviour (TDM smoke + 16/16 scenario checks still pass). Full write-up: `docs/bomb-defusal.md`.

Verification: `node scripts/validate.mjs` — lint and typecheck clean; 148 Node tests, 131 passing — all 27 new Bomb Defusal tests pass and the few remaining failures at the time were pre-existing stale pins, since fixed on main by the wallet/settings entry below; the new defusal mutations were verified separately (10/10 killed). Production build succeeds. Headless: 8 full bot-vs-bot matches via `scripts/defusal-sim.ts` (53% attack round wins, 0 time-outs, plants/defuses/detonations all occurring, no stuck bots) and `scripts/defusal-engine-smoke.ts`, which boots the real Engine on a stub WebGL context and scripts buy, plant (movement lock + payout), defuse (exact 10 s), pickup, spectate and a complete 12-round match with halftime.

## 2026-09-16 — Tactical radar upgrade, armory stat ribbon, new key art

Minimap rebuilt into a real instrument: enemy blips are now directional wedges showing where each hostile is LOOKING (amber while unaware, burning red pulse once they have contact), the dish rim smoulders red during contact, a faint square grid + 45-degree rim ticks make the terrain read as a mapped instrument, north pops brass, an olive chevron rides the rim pointing at the objective even when it's off-dish, and a footer strip reads live bearing / objective distance / hostile count (goes CLEAR when quiet). Armory stage gained a key-figures ribbon under the gun (DMG/RPM/MAG/ADS/RELOAD/SUPPR at a glance). Map key art regenerated at higher fidelity: Sandblast now a golden-hour drone shot with both bridges, souk tarps and the rusted water tower; Town a dawn kasbah in valley mist with the signal keep, kilns and west gate — both graded to their in-game lighting.

Verification: full `node scripts/validate.mjs` (lint, typecheck, 90 Node tests, 34/34 mutations killed) plus production build.

## 2026-09-15 — Menu flow rebuild, cinematic loading, real recoil, distinct gun audio, AWM rework

Main menu rebuilt to the sketch: home screen is title + stacked Missions / Loadout / Settings with new operator character art on the right (maps no longer shown at start). Missions → pick-map screen with Town and Sandblast tiles — hovering a tile hands the ENTIRE screen to a live 3D orbit of that arena (the hovered tile becomes a glass frame, the other recedes); clicking a map opens its mission list (verb-chip objective cards, first one hot), then Deploy → new cinematic loading screen (full-bleed AO aerial with slow push-in, typed sitrep feed, progress rail) → straight into the game. Money counter fixed: the rolling-odometer CSS (.cash-digit/.cash-strip/.cash-sep + up/down flashes) had never shipped, so every digit rendered its whole 0-9 strip inline. Armory: attachment hardpoints are clickable on unowned guns (window shopping) with a proper "buy the gun first" toast on purchase attempts. Recoil made real: base kick nearly doubled, permanent-climb share raised, per-shot horizontal jitter, ADS reduction cut 0.6→0.88, magnified glass amplifies apparent kick by zoom (sqrt(70/fov)), slower 140 ms recovery spring, camera shake on big-bore single shots. Gun audio separated: per-shot pitch scatter everywhere; M416 bright/tight, AK dull sawtooth hammer + receiver rattle, AWM artillery boom with rolling echo + delayed whip-crack, MP paper zip, Vector dry double-tick, SCAR round 7.62 thud, M249 clanky belt rattle, SPAS broadband slam, 1911 mellow pop with slide clacks; new headshot "dink" rings on every head hit. Per-weapon reload choreography: AK rock-and-lock (mag pivots forward, seat-slap counter-rock), AWM roll-right + long bolt stroke, pistols muzzle-up fast drop + slide run, M249 sag + cover pop, PDW twitch reloads, AR tactical chest-height handling — the gun no longer just dips out of frame. Models: M4 magwell flare made flush (no more broken piece through drum mags) and buffer tube now seats into a receiver end plate; AK bayonet lug + cleaning-rod spike deleted and the stock boot now slots into the receiver tang; AWM rebuilt to the real Accuracy International silhouette (slab green stock halves with a daylight thumbhole, chassis spine, spacer-stacked buttpad, separate near-vertical grip, vented slab forend, huge side-ported brake, stubby single-stack mag). Scopes differentiated: 3x/4x/6x now render distinct sight pictures (prism chevron / duplex telescopic with BDC dots / fine mil-dot precision glass with tight exit pupil), per-tier tube masks and zoom tags, and the ADS gun-hide threshold widened (<45°) so a 4x no longer stares into the back of its own scope model. Headshots more forgiving: head sphere 0.24→0.31 plus a neck capsule that counts as head.

Verification: full `node scripts/validate.mjs` (lint, typecheck, 90 Node tests incl. updated home-menu/boot pins, 34/34 mutations killed), production build, and Node smoke-build of the three reworked gun models (all within draw/tri budgets).

## 2026-09-15 — Menu/Armory relayout, universal scopes, MP secondary, streak + hitmarker fixes

Main menu redesigned to fit one screen without scrolling: two large map tiles side by side (recon artwork that cross-fades into a LIVE camera slow-orbiting above the real arena on hover/selection), mission route list removed — objectives now live on the deploy loading screen together with the brief and a spoken mission briefing (Web Speech, unlocked by the Play click). Maps graded apart: Sandblast keeps its hot amber noon; Town gets a cool blue-grey morning (new sky dome ramp, closer haze, low pale sun, mirrored key light). Scope system overhauled PUBG-style: ACOG/Hybrid/Sniper Scope replaced by 2x/3x/4x/6x scopes that fit EVERY weapon and set an absolute ADS FOV (57/40/30/20° ≈ true 2/3/4/6× at 95° base) via the new `adsFovSet` stat; 6x renders the full scope picture on any gun. MP reclassed to the secondary slot (machine-pistol role). M416 spawns genuinely bare — the built-in holo glass/reticle meshes are gone, irons run through the diopter drum, and the HUD no longer paints a phantom red dot for opticless guns. Armory relayout: watermark and bottom chips footer removed, hardpoint slots live as a persistent list in the right panel, bigger stage and closer camera for the gun, "Fielded" renamed "Equipped", and rapid weapon clicks no longer leak a ghost gun (in-flight swap transitions are disposed). Testing economy: wallet topped to $9,999,999 on load. Gameplay polish: kill hitmarker can no longer stick to the screen (timeout + fade), minimap player marker is now a bright signal-orange arrow with a view cone, and streak callouts are specific — Double, Triple, Quad, Penta, Unstoppable.

Verification: full `node scripts/validate.mjs` (lint clean, typecheck, all Node tests incl. updated catalog/optic/menu pins, 34/34 mutations killed) plus production build.

